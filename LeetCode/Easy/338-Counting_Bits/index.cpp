#include <iostream>
#include <vector>

using namespace std;

class Solution
{
public:
  vector<int> countBits(int n)
  {
    vector<int> numBits{};

    for (int i = 0; i <= n; i++)
    {
      int bitCount = 0;
      int num = i;

      while (num > 0)
      {
      }
    }
  }
};