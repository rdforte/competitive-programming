module.exports.catMouseGame = (graph) => {};

const mouseMoves = (graph, node, seen) => {
  if (node === 0) {
  }
  for (let n of graph[node]) {
    if (seen[n]) continue;
  }

  // calls catMoves
};

const catMoves = () => {
  // calls mouseMoves
};
