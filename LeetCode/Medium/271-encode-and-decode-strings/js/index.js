/**
 * Encodes a list of strings to a single string.
 *
 * @param {string[]} strs
 * @return {string}
 */
module.exports.encode = function (strs) {};

/**
 * Decodes a single string to a list of strings.
 *
 * @param {string} s
 * @return {string[]}
 */
module.exports.decode = function (s) {};

/**
 * Your functions will be called as such:
 * decode(encode(strs));
 */
