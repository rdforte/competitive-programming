#include <iostream>

// https://blog.codechef.com/2009/07/02/tutorial-for-small-factorials/

int multiplyNumbers(int a, int b)
{
    int numA[3];
    int numB[3];

    // put digits into array
    while (a >= 1 || b >= 1)
    {
        if (a >= 1)
        {
            int lastDigA = a % 10;
        }
    }
}

int main()
{
    // This does not support really big numbers

    // int t;
    // std::cin >> t;

    // while (t--)
    // {
    //     int n;
    //     std::cin >> n;
    //     int fac = n;
    //     for (int f = n - 1; f > 0; f--)
    //     {
    //         fac *= f;
    //     }
    //     std::cout << fac << "\n";
    // }
    // return 0;

    int t;
    std::cin >> t;

    while (t--)
    {
        int numberLength
    }
}
